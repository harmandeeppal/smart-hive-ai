# Audio-Only Requirements
# Lightweight audio classification (NO OpenCV!)
# Updated to match training environment for audio_model.pkl

numpy==1.26.4               # Upgraded from 1.24.3 (matches training)
scipy==1.11.4               # Upgraded from 1.11.1 (stability)
scikit-learn==1.3.2         # Upgraded from 1.3.0 (closer to 1.7.2)
librosa==0.10.1             # Upgraded from 0.10.0 (bug fixes)
sounddevice==0.4.6          # Audio recording
joblib==1.3.2               # Model serialization (matches training)
soundfile==0.12.1           # Audio file I/O
audioread==3.0.1            # Audio format support
paho-mqtt==1.6.1            # MQTT client
boto3==1.26.137             # AWS SDK
python-dotenv==1.0.0        # Environment variables
pytz==2023.3                # Timezone support
