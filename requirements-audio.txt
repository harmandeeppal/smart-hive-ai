# Audio-Only Requirements
# Lightweight audio classification (no microphone hardware required in DEMO_MODE)

numpy==2.0.2
scikit-learn==1.7.2
librosa==0.10.2
soundfile==0.12.1
scipy==1.13.1
joblib==1.4.2
paho-mqtt==2.1.0
python-dotenv==1.0.1
pytz==2024.2
# sounddevice and boto3 are NOT listed here:
#   sounddevice — hardware microphone only; imported lazily inside record_audio()
#   boto3       — AWS IoT hardware path only; not used in DEMO_MODE
