# Vision-Only Requirements
# CPU-only YOLO v8 queen bee detection
# torch is installed separately in Dockerfile before this file
# to ensure the CPU wheel is used (not CUDA)

numpy==2.0.2
opencv-python-headless==4.10.0.84
ultralytics==8.3.0
paho-mqtt==2.1.0
python-dotenv==1.0.1
pytz==2024.2
