# ML Inference Service Requirements - MINIMAL for inference only
# Optimized for Raspberry Pi with limited storage and CPU
# NOTE: These are the ABSOLUTE MINIMUM dependencies for inference only

# Core dependencies (MUST be compatible versions)
numpy==1.24.3                     # Numerical computing (LOCKED: OpenCV needs 1.x, not 2.x)
opencv-python-headless==4.8.0.76  # Computer vision (headless, no GUI)

# ML inference - LIGHTWEIGHT only
ultralytics==8.0.0                # YOLOv8 object detection (inference mode only)
scikit-learn==1.3.0               # For audio classification

# Audio
librosa==0.10.0                   # Audio feature extraction
sounddevice==0.4.5                # Microphone recording

# AWS and MQTT
boto3==1.26.137                   # AWS SDK
paho-mqtt==1.6.1                  # MQTT client

# Utilities
python-dotenv==1.0.0              # Environment variables
pytz==2023.3                       # Timezone support
