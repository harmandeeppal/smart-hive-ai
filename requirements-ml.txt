# ML Inference Service Requirements - MINIMAL for inference only
# Optimized for Raspberry Pi with limited storage (no training deps)

# Core ML inference (YOLO v8 detection)
ultralytics==8.0.0              # YOLOv8 object detection (inference only)

# Audio processing
scikit-learn==1.3.0             # Machine learning models
librosa==0.10.0                 # Audio feature extraction

# Core dependencies
numpy==1.24.3                   # Numerical computing
opencv-python-headless==4.8.0.76  # Computer vision (headless, no GUI)

# Audio input
sounddevice==0.4.5              # Microphone recording

# AWS and MQTT
boto3==1.26.137                 # AWS SDK
paho-mqtt==1.6.1                # MQTT client

# Utilities
python-dotenv==1.0.0            # Environment variables
pytz==2023.3                     # Timezone support
